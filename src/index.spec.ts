import { expect } from "vitest";
import { test } from "vitest";
import { describe } from "vitest";
import ngTemplateCache from ".";

describe("ng-template-cache", () => {
	test("should have the right structure", () => {
		expect(true).toBe(true);
	});
});
